require_relative 'test_helper'

describe "Slack" do
  it "will repeat the menu with an invalid input" do
    user_input = "blerg"
  end
  
  it "will display a list of users when user types 'list users' regardless of case" do
  end
  
  it "will display a list of channels when user types 'list channels' regardless of case" do
  end
  
  it "will quit when user inputs 'quit' regardless of case" do
  end
end
